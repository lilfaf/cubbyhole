<tr id="<%= item.class.name.downcase %>-<%= item.id %>" <%= "data-link=#{folder_path(item)}" if item.is_a?(Folder) %>>
  <td><%= thumbnail_for(item) %></td>
  <td><%= link_to item.name, (item.is_a?(Folder) ? folder_path(item) : download_asset_path(item)) %></td>
  <td>
    <%= link_to '×', polymorphic_path(item), class: 'close large-icon left', data: { confirm: "Are you sure?" }, method: :delete, remote: true %>
  </td>
</tr>
