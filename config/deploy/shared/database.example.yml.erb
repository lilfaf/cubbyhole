<%= fetch(:rails_env) %>:
  adapter: mysql2
  timeout: 5000
  encoding: utf8
  reconnect: false
  pool: 20
  host: localhost
  database: <%= "#{fetch(:application)}_#{fetch(:rails_env)}" %>
  pool: 5
  username: <%= "#{fetch(:application)}_#{fetch(:rails_env)}" %>
  password:
